# Arsenal Environment Configuration
# Copy this to .env and fill in your values

# OpenAI API Key (required for semantic-code-search and other AI-powered skills)
# Get your key from: https://platform.openai.com/api-keys
# Or copy from your parent project: grep OPENAI_API_KEY ../api/.env
OPENAI_API_KEY=sk-proj-your-key-here

# Database URL for semantic-code-search (default is fine for Docker setup)
# Only change if you need a custom database configuration
DATABASE_URL=postgresql://codesearch:codesearch@postgres:5432/codesearch

# Project root directory (auto-detected as parent of arsenal)
# Override only if you have a non-standard setup
# PROJECT_ROOT=..

# Langfuse configuration (required for langfuse-prompt-and-trace-debugger skill)
# Get your keys from: https://cloud.langfuse.com/settings
# Or copy from your parent project: grep LANGFUSE ../api/.env

# Staging Langfuse
LANGFUSE_PUBLIC_KEY_STAGING=pk-lf-your-staging-public-key  # pragma: allowlist-secret
LANGFUSE_SECRET_KEY_STAGING=sk-lf-your-staging-secret-key  # pragma: allowlist-secret
LANGFUSE_HOST_STAGING=https://langfuse.staging.your-instance.com

# Production Langfuse
LANGFUSE_PUBLIC_KEY_PROD=pk-lf-your-prod-public-key  # pragma: allowlist-secret
LANGFUSE_SECRET_KEY_PROD=sk-lf-your-prod-secret-key  # pragma: allowlist-secret
LANGFUSE_HOST_PROD=https://langfuse.prod.your-instance.com

# Default environment for Langfuse (staging or production)
LANGFUSE_ENVIRONMENT=staging

# Legacy/fallback keys (optional - will use LANGFUSE_ENVIRONMENT to select)
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here  # pragma: allowlist-secret
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here  # pragma: allowlist-secret
LANGFUSE_HOST=https://cloud.langfuse.com

ENVIRONMENT=production

# AWS Profile for CloudWatch Logs (aws-logs-query skill)
# Specify which AWS CLI profile to use for querying logs
# Default: codel
AWS_PROFILE=codel

# Production Database Read-Only Credentials (sql-reader skill)
# Get credentials from AWS Secrets Manager or your database administrator
# These should be read-only credentials for safe production investigation
PGHOST=your-database-host.rds.amazonaws.com
PGPORT=5432
PGDATABASE=your_database_name
PGUSER=readonly_user
PGPASSWORD=your_readonly_password
PGSSLMODE=require
