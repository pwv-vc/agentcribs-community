[project]
name = "langfuse-prompt-and-trace-debugger"
version = "0.1.0"
description = "Standalone Langfuse prompt and trace viewer skill"
requires-python = ">=3.9"
dependencies = [
    "langfuse==2.60.3",
    "httpx==0.27.2",
]

[tool.uv]
package = false
