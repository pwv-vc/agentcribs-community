# This skill now uses centralized configuration
# See ../../../.env.example at the superpowers root directory

# To configure:
# 1. cd to superpowers root: cd ../../..
# 2. cp .env.example .env
# 3. Edit .env with your OPENAI_API_KEY
# 4. Run: docker-compose up -d
